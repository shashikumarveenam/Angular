import { LableColorDirective } from './lable-color.directive';

describe('LableColorDirective', () => {
  it('should create an instance', () => {
    const directive = new LableColorDirective();
    expect(directive).toBeTruthy();
  });
});
